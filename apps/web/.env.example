# ============================================================
# PAPERMARKET WEB — variáveis de ambiente
# Copie para .env.local e preencha os valores
# ============================================================

# URL base do backend Rust
NEXT_PUBLIC_API_URL=http://localhost:8080

# URL WebSocket para eventos em tempo real
NEXT_PUBLIC_WS_URL=ws://localhost:8080/ws
```

---

Esses eram todos os arquivos de configuração base! ✅

Resumo do que foi criado até agora:
```
papermarket/
├── .gitignore
├── Cargo.toml
├── package.json
├── pnpm-workspace.yaml
├── apps/
│   ├── api/
│   │   ├── Cargo.toml
│   │   └── .env.example
│   └── web/
│       ├── package.json
│       ├── next.config.ts
│       ├── tailwind.config.ts
│       ├── tsconfig.json
│       ├── postcss.config.js
│       └── .env.example